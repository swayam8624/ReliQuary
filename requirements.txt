# Runtime API
fastapi==0.116.1
uvicorn[standard]==0.35.0
pydantic==2.11.7
langchain==0.3.27
langgraph==0.6.2
python-multipart==0.0.20
requests==2.32.4
httpx==0.28.1
PyYAML==6.0.2
psycopg[binary]==3.2.10
cryptography==45.0.5
cbor2==5.6.5
email-validator==2.3.0
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
psutil==7.1.3

# Optional WebAuthn support. The app falls back to simulation mode if this is
# unavailable, but installing it keeps the local API closer to the full demo.
fido2==2.0.0
webauthn==2.1.0

# Local verification
pytest==8.4.1
pytest-asyncio==1.1.0

# Rust/PyO3 build helper used by scripts/build_rust_modules.sh
maturin==1.9.2
